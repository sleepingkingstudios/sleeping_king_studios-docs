---
name: SleepingKingStudios::Docs::Commands::Generate#initialize
parent_path: sleeping-king-studios/docs/commands/generate
signature: 'initialize(docs_path:, error_stream: $stderr, output_stream: $stdout,
  **options)'
slug: initialize
constructor: true
data_path: sleeping-king-studios/docs/commands/generate/i-initialize
overloads:
- name: SleepingKingStudios::Docs::Commands::Generate#initialize
  parent_path: sleeping-king-studios/docs/commands/generate
  signature: initialize(docs_path:, **options)
  slug: initialize
  constructor: true
  options:
  - name: options
    opts:
    - description: |-
        if true, does not make any changes to
        the filesystem.
      name: dry_run
      type:
      - name: Boolean
    - description: if true, overwrites any existing files.
      name: force
      type:
      - name: Boolean
    - description: |-
        the code version for the generated
        documentation.
      name: version
      type:
      - name: String
    - description: |-
        if true, prints status messages to
        STDOUT.
      name: verbose
      type:
      - name: Boolean
  params:
  - description: |-
      the directory path for generating the
      documentation files.
    name: docs_path
    type:
    - name: String
  - description: the configured options for the generator.
    name: options
    type:
    - name: Hash
returns:
- description: a new instance of Generate
  type:
  - name: Generate
version: '0.2'
